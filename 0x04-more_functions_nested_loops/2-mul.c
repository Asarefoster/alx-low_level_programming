#include "main.h"

/**
*mul - multiplies two integers
*@a: the first numebr to multiply
*@b: the second number to multiply
*Return: The result of multiplication between x param and y param
*/

int mul(int a, int b)
{
	return (a * b);
}
